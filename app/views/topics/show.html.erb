<br/><p id="notice"><%= notice %></p>

<h1>Thème "<%= @topic.title %>"</h1>

<hr/>
<%= link_to 'Voir tous les thèmes', topics_path %>
<hr/>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th>Titre</th>
      <th>Description</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @topic.title %></td>
      <td><%= @topic.description %></td>
      <td><%= link_to 'Editer', edit_topic_path(@topic) %></td>
    </tr>
  </tbody>
</table>
