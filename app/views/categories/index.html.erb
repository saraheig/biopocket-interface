<p id="notice"><%= notice %></p>

<h1>Table des catégories de temps (pour les tâches -> périodes)</h1>
<em>
  Cette table concerne les types de périodes des tâches.<br/>
  Une période (table "Périodes") n'a qu'une seule catégorie de temps, tandis qu'une catégorie de temps peut correspondre à plusieurs périodes.
</em>

<hr/>
<%= link_to 'Ajouter une nouvelle catégorie de temps', new_category_path %>
<hr/>

<h3>Effectuer une recherche</h3><br/>
<%= form_tag(categories_path, method: :get) do %>

  <div class="field">
    Mot-clé cherché dans le titre :
    <%= text_field_tag :search, params[:search], size: '30', placeholder: 'Aucun mot-clé' %>
  </div>

  <div class="actions">
    <%= submit_tag 'Rechercher', name: nil %>
  </div>

<% end %><br/>

<%= link_to 'Annuler la recherche', categories_path %><br/><br/>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th>Titre de la catégorie de temps</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.title %></td>
        <td>
          <%= link_to 'Editer', edit_category_path(category) %><br/>
          <%= link_to 'Supprimer', category, method: :delete, data: { confirm: 'Etes-vous sûr ? Attention : La suppression de cette catégorie de temps est irréversible et suprimera toutes les périodes (des tâches) (table "Périodes") associées.' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
