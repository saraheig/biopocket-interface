<p id="notice"><%= notice %></p>

<h1>Table des photos (pour les tâches)</h1>
<em>
  Cette table contient les liens des photos relatives aux tâches.<br/>
  Une tâche (table "Tâches") peut avoir plusieurs photo. En revanche, une ligne de cette table ne correspond qu'à une seule tâche (pas à plusieurs tâches). 
</em>

<hr/>
<%= link_to 'Ajouter une nouvelle photo', new_picture_path %>
<hr/>

<h3>Effectuer une recherche</h3><br/>
<%= form_tag(pictures_path, method: :get) do %>

  <%= text_field_tag :search, params[:search], size: '30', placeholder: 'Aucun mot-clé' %>
  <%= submit_tag 'Rechercher', name: nil %>

<% end %><br/>

<%= link_to 'Annuler la recherche', pictures_path %><br/><br/>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th>Nom de la photo</th>
      <th>Description</th>
      <th>Tâche (Action - Thème)</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @pictures.each do |picture| %>
      <tr>
        <td><%= picture.picture %></td>
        <td><%= picture.description %></td>
        <td><%= picture.task.title %> (<%= picture.task.action.title %> - <%= picture.task.action.theme.title %>)</td>
        <td>
          <%= link_to 'Editer', edit_picture_path(picture) %><br/>
          <%= link_to 'Supprimer', picture, method: :delete, data: { confirm: 'Etes-vous sûr ? Attention : La suppression de cette photo est irréversible.' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>